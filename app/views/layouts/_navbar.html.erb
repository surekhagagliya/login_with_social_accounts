<nav class="navbar navbar-expand-sm rounded navbar-custom">
    <a href="/" class="navbar-brand">Social Login</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCustom">
        <i class="fa fa-bars fa-lg py-1 text-white"></i>
    </button>
    <div class="navbar-collapse collapse" id="navbarCustom">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="#">Home</a>
            </li>
        </ul>
        <% if user_signed_in? %>
            <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class:"ml-auto navbar-text" %>
        <% else %>
            <%= link_to 'Sign in', new_user_session_path, class:"ml-auto navbar-text" %>
        <% end %>
    </div>
</nav>
<br>